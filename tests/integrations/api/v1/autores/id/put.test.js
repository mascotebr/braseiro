test('Ter atualizado o autor', async () => {
  // const url = 'http://localhost:3000/api/v1/autores/'
  // const autorToEdited = {
  //   nome_artistico: 'Nome Teste',
  //   slug: 'teste',
  //   bio: 'Teste maior bio',
  //   email: 'teste@gmail.com',
  // }
  // const id = 1
  // const autorBeforeResponse = await fetch(`${url}${autorToEdited.slug}`)
  // const autorBefore = await autorBeforeResponse.json()
  // const autorEditedResponse = await fetch(`${url}id/${id}`, {
  //   method: 'PUT',
  //   headers: {
  //     'Content-Type': 'application/json',
  //   },
  //   body: JSON.stringify(autorToEdited),
  // })
  // expect(autorEditedResponse.status).toBe(201)
  // const autorAfterResponse = await fetch(`${url}${autorToEdited.slug}`)
  // const autorAfter = await autorAfterResponse.json()
  // expect(autorAfter.nome_artistico).toBe(autorBefore.nome_artistico)
  // expect(autorAfter.slug).toBe(autorBefore.slug)
  // expect(autorAfter.bio).toBe(autorBefore.bio)
  // expect(autorAfter.email).toBe(autorBefore.email)
})
